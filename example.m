clear all; close all;pkg load image;load('images.mat');% 20130927testImage = squeeze(imgs(:,:,1));figure();imagesc(imresize(testImage, [1024 512], 'cubic'));colormap gray;set(gca, 'xtick', [], 'ytick', []);NTE = 20;TE = 0.013;TElist = (1:NTE) * TE;T2Range = [.010 20];T2Samples = 200;doPlot = 1;regularization = 0;testpixel = squeeze(imgs(64, 42, :));testpixel = squeeze(imgs(64, 18, :));[T2Spectra, k, sfit] = t2nnls(testpixel, TElist, doPlot, T2Range, T2Samples, regularization);